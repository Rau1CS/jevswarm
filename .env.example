# Copy to .env.local and fill in. Read ONLY by the local Vite server (server/jevProxy.ts);
# it is never bundled into browser code.
TYPESAFE_API_KEY=
# Optional: pin a model version (defaults to jev-latest)
TYPESAFE_MODEL=jev-latest
